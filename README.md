# Flight-Booking-Records
This C++ program manages flight booking records using Indexed Sequential File (ISF). It stores booking details and maintains an index file mapping booking IDs to file positions. It supports adding bookings, searching by booking ID, and displaying all records efficiently.
